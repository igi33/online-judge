<Application x:Class="OnlineJudgeWpfApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:OnlineJudgeWpfApp"
             xmlns:cv="clr-namespace:OnlineJudgeWpfApp.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <cv:BooleanToVisibilityConverter
            x:Key="BoolToVis"
            True="Visible" 
            False="Collapsed" />
        <cv:BooleanToVisibilityConverter
            x:Key="BoolToVisInv"
            True="Collapsed" 
            False="Visible" />
        <Style x:Key="CellElementStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="7" />
        </Style>
        <Style x:Key="DataGridCellStyle" TargetType="{x:Type DataGridCell}">
            <Style.Triggers>
                <Trigger Property="DataGridCell.IsSelected" Value="True">
                    <Setter Property="BorderBrush">
                        <Setter.Value>
                            <SolidColorBrush Color="Transparent"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Foreground"
                            Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                    <Setter Property="Background">
                        <Setter.Value>
                            <SolidColorBrush Color="Transparent"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="BaseFontStyle">
            <Setter Property="Control.FontSize" Value="14" />
            <Setter Property="Control.FontFamily" Value="Microsoft Sans Serif" />
        </Style>
        <Style x:Key="FontStyle" TargetType="{x:Type Control}" BasedOn="{StaticResource BaseFontStyle}">
        </Style>
        <Style x:Key="PageFontStyle" TargetType="{x:Type Page}" BasedOn="{StaticResource BaseFontStyle}">
        </Style>
    </Application.Resources>
</Application>
